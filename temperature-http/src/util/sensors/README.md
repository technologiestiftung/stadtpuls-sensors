little script that uses Deno to write files to fs based on a template data taken from our db.

install deno for your platform: https://deno.land/#installation

run the script

```bash
deno run --allow-write --allow-read index.ts
```